# TEMPORARY: Minimal CI for build optimization testing
# Only runs prepare_hived_image to measure build times

stages:
- build

variables:
  GIT_DEPTH: 15
  GIT_SUBMODULE_DEPTH: 1
  GIT_STRATEGY: clone
  GIT_SUBMODULE_STRATEGY: recursive

include:
  - template: Workflows/Branch-Pipelines.gitlab-ci.yml
  - local: '/scripts/ci-helpers/ci_image_tag_vars.yml'
  - local: '/scripts/ci-helpers/prepare_data_image_job.yml'

prepare_hived_image:
  extends: .prepare_hived_image
  stage: build
  variables:
    REGISTRY_USER: "$HIVED_CI_IMGBUILDER_USER"
    REGISTRY_PASS: $HIVED_CI_IMGBUILDER_PASSWORD
    BINARY_CACHE_PATH: "$CI_PROJECT_DIR/hived-binaries"
    HIVE_NETWORK_TYPE: mainnet
  tags:
    - public-runner-docker
    - hived-for-tests
    - hived
