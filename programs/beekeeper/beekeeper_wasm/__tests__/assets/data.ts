export const STORAGE_ROOT = '/storage_root';
export const WALLET_OPTIONS = ['--wallet-dir', `${STORAGE_ROOT}/directory with spaces/.beekeeper`, '--enable-logs', 'false'];

export const STORAGE_ROOT_NODE = '.beekeeper';
export const WALLET_OPTIONS_NODE = ['--wallet-dir', `${STORAGE_ROOT_NODE}/directory with spaces/.beekeeper`, '--enable-logs', 'false'];
