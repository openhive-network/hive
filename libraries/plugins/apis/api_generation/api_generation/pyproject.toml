[build-system]
requires = ["poetry-core==2.1.3"]
build-backend = "poetry.core.masonry.api"


[tool.poetry]
requires-poetry = "2.1.3"
name = "api_generation"
version = "0.0.0"
description = ""
authors = []

source = [
    { name = "PyPI", priority = "primary" },
    { name = "gitlab-hive", url = "https://gitlab.syncad.com/api/v4/groups/136/-/packages/pypi/simple", priority = "primary" },
]


[tool.poetry.dependencies]
python = ">=3.14,<4"
jinja2 = "^3.1.6"
hiveio-beekeepy = "^1.28.6-post.0"  # NOTE: Also update python_api_package/pyproject.toml when changing this version
hiveio-api-client-generator = "^1.7.1-post.0"
