[build-system]
requires = [
    "poetry-core>=1.7.0",
    "poetry-dynamic-versioning>=1.0.0,<2.0.0",
]
build-backend = "poetry_dynamic_versioning.backend"


[tool.poetry]
name = "api_generation"
version = "0.0.0"
description = "Common api generator"
authors = []

packages = [
    { include = "api_generation" }
]
include = [
    { path =  "api_generation/*", format = ["wheel", "sdist"] }
]


[tool.poetry.dependencies]
python = "^3.12"
schemas = { version = "0.0.1.dev367+fa1d763", source = "gitlab-schemas" }
beekeepy = { version = "0.0.1.dev395+8ff3146", source = "gitlab-beekeepy" }


[tool.poetry-dynamic-versioning]
enable = true
format-jinja = """
    {%- set dirty_postfix = '.dirty' if dirty else '' -%}
    {%- if distance == 0 and not dirty -%}
        {{ serialize_pep440(base, stage, revision) }}
    {%- elif revision is not none -%}
        {{ serialize_pep440(base, stage, revision + 1, dev=distance, metadata=[commit]) }}{{ dirty_postfix }}
    {%- else -%}
        {{ serialize_pep440(bump_version(base), stage, revision, dev=distance, metadata=[commit]) }}{{ dirty_postfix }}
    {%- endif -%}
"""

[tool.poetry-dynamic-versioning.substitution]
files = [
    "api_generation/__init__.py"
]