from __future__ import annotations

from typing import TypeVar

from msgspec import UNSET, UnsetType


T = TypeVar("T")


def optional_field(value: T | UnsetType) -> T | None:
    """Return None when an optional response field was omitted by the API."""
    if value is UNSET:
        return None
    return value
