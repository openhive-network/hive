[build-system]
requires = ["poetry-core==1.1.0"]
build-backend = "poetry.core.masonry.api"


[tool.poetry]
name = "shared-tools"
version = "0.0.0"
description = "Tools shared by tests in hive projects"
authors = ["Mariusz Trela"]
packages = [
    { include = "shared_tools" },
]


[tool.poetry.dependencies]
python = "^3.10"
cython = "0.29.34"
setuptools = "59.6.0"
scikit-build = "0.17.2"