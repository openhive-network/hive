[pytest]
addopts = --strict-markers
log_cli = true
log_level = INFO
log_format = %(asctime)s [%(levelname)s] %(message)s (%(filename)s:%(lineno)s)
log_date_format = %Y-%m-%d %H:%M:%S
markers =
    testnet
    mirrornet
    mainnet_5m
    live_mainnet
    replayed_node
    decorated_with_run_for
