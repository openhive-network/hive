---
  test_name: Hived get_account_count

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_account_count
      request:
        url: "{service.url:s}"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_account_count"
          params: []
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "get"
            directory: "condenser_api_patterns/get_account_count"
