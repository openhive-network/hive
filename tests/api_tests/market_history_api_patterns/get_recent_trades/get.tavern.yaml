---
  test_name: Hived market_history_api.get_recent_trades

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: market_history_api.get_recent_trades
      request:
        url: "{service.url:s}"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "market_history_api.get_recent_trades"
          params: {"limit": 500}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "get"
            directory: "market_history_api_patterns/get_recent_trades"
            ignore_tags: ['trades']