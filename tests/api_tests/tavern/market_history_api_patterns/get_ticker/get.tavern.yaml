---
  test_name: Hived market_history_api.get_ticker

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: market_history_api.get_ticker
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "market_history_api.get_ticker"
          params: {"limit": 500}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "get"
            directory: "market_history_api_patterns/get_ticker"
            ignore_tags: ['hbd_volume','highest_bid','hive_volume','latest','lowest_ask','percent_change']