---
  test_name: Hived rc_api.get_resource_params

  marks:
    - patterntest
    # "Unknown error - parsing rpc message failed"

  includes:
    - !include ../../common.yaml

  stages:
    - name: rc_api.get_resource_params
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "rc_api.get_resource_params"
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "get"
            directory: "rc_api_patterns/get_resource_params"
            ignore_tags: ["resource_params","size_info"]