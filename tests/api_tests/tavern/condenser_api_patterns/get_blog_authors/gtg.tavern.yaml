---
  test_name: Hived get_blog_authors

  marks:
    - patterntest
    - xfail # _follow_api: follow_api_plugin not enabled.

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_blog_authors
      request:
        url: "{service.url:s}"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_blog_authors"
          params: ["gtg"]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "gtg"
            directory: "condenser_api_patterns/get_blog_authors"
