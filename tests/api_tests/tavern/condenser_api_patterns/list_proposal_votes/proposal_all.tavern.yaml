---
  test_name: Hived condenser_api.list_proposal_votes

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: condenser_api.list_proposal_votes
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.list_proposal_votes"
          params: [[10], 10, "by_proposal_voter", "ascending", "all"]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "proposal_all"
            directory: "condenser_api_patterns/list_proposal_votes"
