---
  test_name: Hived condenser_api.lookup_witness_accounts

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: condenser_api.lookup_witness_accounts
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.lookup_witness_accounts"
          params: ["", 1000]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "max_limit"
            directory: "condenser_api_patterns/lookup_witness_accounts"