---
  test_name: Hived condenser_api.get_withdraw_routes all

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: condenser_api.get_withdraw_routes all
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_withdraw_routes"
          params: ["hiveio", "all"]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "all"
            directory: "condenser_api_patterns/get_withdraw_routes"