---
  test_name: Hived condenser_api.lookup_accounts

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: condenser_api.lookup_accounts
      request:
        url: "{service.url:s}"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.lookup_accounts"
          params: ["gtg", 1]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "get"
            directory: "condenser_api_patterns/lookup_accounts"