---
  test_name: Hived

  marks:
    - patterntest # not found yet, something happened near 3334000 block

  includes:
    - !include ../../common.yaml

  stages:
    - name: test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_account_history"
          params: ["gtg",100,100,32768]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "custom_operation"
            directory: "condenser_api_patterns/get_account_history"
