---
  test_name: Hived get_owner_history

  marks:
    - patterntest #todo find is there any owner history?

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_owner_history
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_owner_history"
          params: ["gtg"]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "get"
            directory: "condenser_api_patterns/get_owner_history"