---
  test_name: Hived get_active_votes

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_active_votes
      request:
        url: "{service.url:s}"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_active_votes"
          params: ["gtg","wrong_permlink"]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "wrong_permlink"
            directory: "condenser_api_negative/get_active_votes"
            error_response: true
            ignore_tags: ["timestamp"]