---
  test_name: Hived

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "database_api.list_proposal_votes"
          params: {"start":["gtg"], "limit":10, "order":"by_voter_proposal", "order_direction": "ascending", "status": "inactive"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "by_voter_proposal_inactive"
            directory: "database_api_patterns/list_proposal_votes"
            ignore_tags: ['proposal_votes']