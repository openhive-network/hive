---
  test_name: Hived account_history_api get_account_history filter to limit_order_create_operation

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: account_history_api get_account_history limit_order_create_operation
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "account_history_api.get_account_history"
          params: {"account":"adm","operation_filter_low": 32,"start": 1000,"limit": 1000}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "limit_order_create_operation"
            directory: "account_history_api_patterns/get_account_history"
