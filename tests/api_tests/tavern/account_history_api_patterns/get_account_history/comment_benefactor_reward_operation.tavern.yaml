---
  test_name: Hived account_history_api comment_benefactor_reward_operation

  marks:
    - patterntest # virtual
    # no noempty results

  includes:
    - !include ../../common.yaml

  stages:
    - name: account_history_api comment_benefactor_reward_operation
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "account_history_api.get_account_history"
          params: {"account":"blocktrades", "operation_filter_low": 2305843009213693952, "start": 1000, "limit": 1000}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "comment_benefactor_reward_operation"
            directory: "account_history_api_patterns/get_account_history"
