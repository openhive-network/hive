---
  test_name: Hived account_history_api get_ops_in_block only virtual true

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: account_history_api get_ops_in_block only virtual true
      request:
        url: "{service.url:s}"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "account_history_api.get_ops_in_block"
          params: {"block_num": 2797050, "only_virtual": true}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "only_virtual_true"
            directory: "account_history_api_patterns/get_ops_in_block"
