# CI Optimization Task Report - hive-test-speedup-v4

## Summary

This task focused on optimizing the CI pipeline for the Hive project to reduce build and test execution times. The primary goal was to identify and implement optimizations that speed up the CI pipeline without reducing test coverage.

## Changes Made

### Iteration 1: Increase Pytest Parallelism
**Commit:** `2ecb4310d` - CI: Increase default PYTEST_NUMBER_OF_PROCESSES from 8 to 12 for faster test execution

**Change Details:**
- Modified `.gitlab-ci.yaml` line 10
- Changed `PYTEST_NUMBER_OF_PROCESSES: 8` to `PYTEST_NUMBER_OF_PROCESSES: 12`
- This affects all pytest-based test jobs that use the default parallelism setting

**Rationale:**
- Modern CI runners typically have 16+ cores available
- Pytest-xdist can efficiently distribute tests across more workers
- The 50% increase (8 -> 12 processes) provides meaningful speedup while leaving headroom for system processes

**Pipeline:** https://gitlab.syncad.com/hive/hive/-/pipelines/140508 (running)

## Analysis Findings

### Current Pipeline Structure
The CI pipeline has the following stages:
1. `static_code_analysis` - Pre-commit checks
2. `build` - Multiple build jobs (hived images, beekeeper, block logs, API packages)
3. `test` - ~50+ parallel test jobs
4. `cleanup` - Cache cleanup
5. `publish` - Package deployment
6. `deploy` - Docker image and wheel deployment

### Key Parallelism Settings
| Setting | Value | Notes |
|---------|-------|-------|
| Default `PYTEST_NUMBER_OF_PROCESSES` | 12 (was 8) | Affects most test jobs |
| `fork_tests` | 3 processes | Limited due to resource-intensive network simulations |
| `comment_cashout_tests` | 1 process | Appears to have ordering dependencies |
| `colony_tests` | 1 process | Sequential test requirements |

### Optimization Opportunities Identified

#### 1. Already Implemented
- **Pytest parallelism increase**: Changed default from 8 to 12 processes

#### 2. Recommended Future Optimizations

**A. Increase fork_tests parallelism (Conservative)**
```yaml
fork_tests:
  variables:
    PYTEST_NUMBER_OF_PROCESSES: 4  # Currently 3
```
The fork tests create complex network topologies but could potentially handle slightly more parallelism.

**B. Enable PYTEST_LOG_DURATIONS for profiling**
```yaml
variables:
  PYTEST_LOG_DURATIONS: 1
```
This would output test timing data to identify the slowest tests for targeted optimization.

**C. Build caching improvements**
- The `generate_testing_block_logs` job runs 10 parallel matrix jobs that could benefit from better caching
- Docker layer caching is partially implemented but could be expanded

**D. Job dependency optimization**
Some test jobs wait for builds they don't directly need. Reviewing `needs:` dependencies could allow earlier job starts.

**E. Sleep call analysis**
Found 12 files with `sleep()` calls in test code:
- `tests/python/hive-local-tools/hive_utils/common.py` - Used for block waiting
- `tests/python/functional/fork_tests/` - Network synchronization
- Most are legitimate waiting patterns for blockchain state changes

### Files Modified
- `.gitlab-ci.yaml` - Main CI configuration

## Results

Pipeline 140508 is currently running with the increased parallelism setting. Expected outcomes:
- Estimated 10-20% reduction in pytest job execution times
- No impact on test reliability (parallelism is already well-supported)
- Memory usage may increase slightly but should remain within runner limits

## Issues Encountered

1. **Pipeline duration**: Full CI pipelines take 1-2 hours, limiting the number of iterations possible within time constraints
2. **Token permissions**: Some GitLab API endpoints returned 403 for detailed job information

## Recommendations for Further Work

1. **Monitor pipeline 140508** - Verify all tests pass with 12 processes
2. **Consider further parallelism increase** - If successful, test with 16 processes
3. **Profile individual test suites** - Enable `PYTEST_LOG_DURATIONS` to identify slow tests
4. **Review fork_tests bottleneck** - These tests run with only 3 processes due to resource concerns
5. **Implement test splitting** - Long-running test suites could be split into more matrix jobs
6. **Cache optimization** - Review block log generation caching to avoid redundant work

## Technical Details

### Branch Information
- **Branch:** `ci-speedup-hive-v4`
- **Base:** `develop`
- **Project:** 198 (hive/hive)

### Commits
1. `2ecb4310d` - CI: Increase default PYTEST_NUMBER_OF_PROCESSES from 8 to 12 for faster test execution

---
*Generated by CI Optimization Task on 2025-11-30*
